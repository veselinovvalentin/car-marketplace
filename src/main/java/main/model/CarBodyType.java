package main.model;

public enum CarBodyType {
        VAN,
        SUV,
        CABRIOLET,
        WAGON,
        COUPE,
        MINIVAN,
        PICKUP,
        SEDAN,
        STRETCH_LIMOUSINE,
        HATCHBACK

}
